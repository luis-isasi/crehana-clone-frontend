import ModalBase from '@Modals/ModalBase';

const ModalCategoriesMovil = () => {
  return (
    <ModalBase className="bg-gray-500">
      Desde el modal para las categorias en movil
    </ModalBase>
  );
};

export default ModalCategoriesMovil;
