import ExpandMoreIcon from '@Components/Icons/ExpandMoreIcon';

const FilterBy = () => {
  return (
    <button className="flex-grow h-full font-bold">
      Recomendados
      <span className="transform rotate-45 bg-blue-500">ASFSAF</span>
    </button>
  );
};

export default FilterBy;
